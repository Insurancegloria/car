# 保障小卡製作所

一個可直接在瀏覽器開啟的保單保障摘要產生器。輸入保戶資料、標題與各項保障額度後，右側海報會立即更新；按下 **下載 JPG 圖檔** 即可取得 1200 × 1600 px 的圖片。

## 使用方式

不需安裝相依套件。直接以瀏覽器開啟 `index.html`，或在專案目錄執行：

```bash
python3 -m http.server 4173
```

接著前往 `http://localhost:4173`。在左側表單修改文字或數字，確認右側預覽後下載 JPG 檔案。
